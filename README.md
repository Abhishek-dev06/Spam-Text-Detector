# Spam-Text-Detector
The Machine Learning model is trained using the SMS Spam Collection Dataset. Text data is preprocessed by converting it to lowercase and removing punctuation, stopwords, and unnecessary characters. The cleaned text is then converted into numerical features using TF-IDF (Term Frequency–Inverse Document Frequency).
